{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "docker-php-ext-install zip gd && composer install --optimize-autoloader --no-scripts --no-interaction && npm install && npm run production"
  },
  "deploy": {
    "startCommand": "php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}